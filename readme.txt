* all Jupyter Notebooks should be in the folder called
* output will be in output\P1_task1.pickle
* run the following command
python action_tagging.py experiment output\P1_task1.pickle

Comment: the the filename to be suitable to the participant_num and task_num

* Modify input and output file names (to be suitable to the participant_num and task_num) in researchMapping.py
* run the script researchMapping.py in Spyder for creating dataFrame
* the output will be in output\P1_task1_df.pickle